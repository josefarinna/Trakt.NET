name: "Code Scan"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 22 * * 5'

jobs:
  codeql:
    name: "Code-QL"
    runs-on: windows-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['csharp']
        configuration: [Release]

    steps:
    - name: Checkout
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      with:
        fetch-depth: 0

    - name: Initialize CodeQL
      uses: github/codeql-action/init@c4dd10e44af883a891fe31ced449bcb4a6728b9b # v4.37.3
      with:
        languages: 'csharp'

    - name: Restore dependencies
      run: dotnet restore
      working-directory: 'src'

    - name: Build solution
      run: dotnet build  --configuration Release --no-restore
      working-directory: 'src'

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@c4dd10e44af883a891fe31ced449bcb4a6728b9b # v4.37.3
